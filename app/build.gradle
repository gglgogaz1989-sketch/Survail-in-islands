android {
    compileSdk 34

    namespace "com.gglgog.survailinislands" // <-- добавляем сюда

    defaultConfig {
        applicationId "com.gglgog.survailinislands"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
