android {
    namespace "com.gglgog.survailinislands"  // <--- сюда имя пакета из манифеста
    compileSdk 34

    defaultConfig {
        applicationId "com.gglgog.survailinislands"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
